# Backend environment variables for Bear
PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/bear_app
JWT_SECRET=please_change_me_to_a_long_random_string

# SMTP settings (use a real SMTP to send emails)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="Bear <no-reply@bear.app>"
